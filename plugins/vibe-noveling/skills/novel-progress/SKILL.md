---
name: novel-progress
description: 小说创作进度可视化。当用户需要查看创作进度、字数统计、饼图、进度表、progress、字数占比、各类型文件统计时触发。无论用户说的是"进度"、"字数"、"饼图"还是"当前写了多少"，都应该触发此技能。
---

# 小说进度可视化

生成一个 HTML 饼图，展示项目中各类型文件的字数占比。

## 工作方式

运行 `scripts/progress_chart.py`，它会自动：
1. 扫描项目目录，按类型统计字数
2. 在项目根目录生成 `progress.html`
3. 用浏览器打开

## 文件分类规则

| 分类 | 匹配路径 | 说明 |
|------|----------|------|
| 大纲 | `chapters/*-outline.md` | 各章场景化剧情大纲 |
| 设定文件 | `memory/**`, `chapters/*-context.md` | 世界观、角色、物品等设定 + 章节上下文 |
| 正文 | `chapters/ch-XXX.md`（不含 `-outline` 和 `-context` 后缀） | 已撰写的章节正文 |

## 字数统计规则

- 统计中文字符、标点、数字、英文字母
- 排除空白字符和 Markdown 语法标记（`#`、`*`、`-`、`>`、`|`、`` ` ``、`---`）
- 排除 YAML frontmatter（`---` 之间的内容）

## 用户参数

用户可以指定 `--output` 路径或 `--open false` 不自动打开浏览器。
默认：输出到项目根目录 `progress.html` 并自动打开。
