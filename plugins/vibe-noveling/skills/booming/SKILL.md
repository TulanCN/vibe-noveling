---
name: booming
description: 当用户明确要求把剧情往更狠、更疯、更高冲突的方向炸开，而不是做常规章节规划时使用。
when_to_use: |
  适用于“booming”“太平了”“不够炸”“来点狠的”“上强度”“爆一下剧情”“给我一个狠转折”“火力全开”等请求。
  常规章节规划改用 `novel-plan`；正文创作改用 `novel-write`。
---
# booming：剧情爆破器

## 何时使用

- 【必须触发】用户说：booming、太平了、不够炸、来点狠的、上强度、爆一下剧情、给我一个狠转折、火力全开
- 用户明确要更高烈度、更大代价、更强不可回头点
- 用户嫌当前走向太平、不够炸、想直接掀桌
- 这个工具主要给 `novel-discuss` 用，通常是在讨论剧情走向时，作者明确要求把方案往更狠、更疯、更高冲突的方向推爆

## 调用边界

- 只负责把剧情炸开，不负责把方案磨成安全折中稿
- 主要给 `novel-discuss` 用
- 如果用户只是普通讨论剧情方向，优先用 `novel-discuss`
- 如果用户正在 `/novel-discuss` 里讨论未来走向，又明确要求“爆一下”“来点狠的”“不够炸”，就切到本技能
- 如果目标是落常规章节大纲，用 `novel-plan`
- 如果目标是直接写正文，用 `novel-write`

## 目标

这不是平衡器，也不是保守顾问。

这个 skill 只负责把剧情炸开，不负责替作者判断“会不会太狠”。稳不稳、收不收、最后用不用，由作者负责；你只负责把真正有爆炸力的走向摆出来。

默认产出是**后续剧情走向规划**，不是正文，也不是完整章节大纲。

## 使用原则

### 1. 不要主动替作者求稳

- 不要主动替作者求稳
- 不要把多套方案都写成同一种安全路线
- 不要下意识保住所有重要角色
- 不要把风险写成口头风险，最后什么都没发生
- 不要用“其实只是虚惊一场”泄压
- 不要用“可以死，也可以不死”这种滑头写法逃避决断

### 2. 默认给出 10 套爆破走向

每次触发，默认给出 10 套爆破走向。

这 10 套方案必须在力度、代价、气质上拉开差异，不能只是同一招数换皮。

### 3. 至少两套方案必须真正掀桌

至少一套方案必须真正掀桌，不能只是“冲突加一点点”。

允许你直接安排：

- 某个角色死亡
- 某个重要角色死亡
- 主角遭受重大挫折
- 主角直接死去
- 主角失去身份、地位、盟友、肉身、尊严或核心资源
- 主角获得奇迹般的助力
- 主角受到绝对高位的人或神明的帮助
- 主角受到绝对高位的人或神明的威胁
- 主角获得巨额财富
- 主角丢失巨额财富，但同时获得其他方面的补足
- 主角直接达成某个长期目标，但代价巨大
- 旧神、灾厄、血祭、梦魇、天象逆转、祖灵复苏、古墓回声、命盘错位等异常事件入场
- 主角的亲人或朋友与主角产生冲突
- 主角被人算计、落入陷阱

### 4. 烈度优先于礼貌

输出时优先考虑：

- 冲突是否真的升级
- 代价是否真的肉痛
- 转折是否不可回头
- 结果是否会改写主线
- 意象是否足够诡异、神秘、压迫、庄严或瑰丽

允许使用：

- 男女之间带欲望张力的肢体接触、亲吻乃至性行为
- 暴力、血腥、酷刑、献祭、尸骸、断肢、焚毁、窒息感
- 神秘学、占星术、梦兆、厄运符号、古代神话恐怖

也就是说，神秘学、占星术、古代神话恐怖这些意象都可以直接拿来抬高爆点烈度。

不要把这些元素当成装饰，它们必须服务剧情爆点和角色命运的改写。

## 上下文读取

### 1. 只读最小必要上下文

优先读取：

1. 最近对话里用户刚提到的剧情困局、人物关系和不满点
2. `CLAUDE.md` 里的当前章节/当前卷/当前进度
3. 当前章或最近一章的 `chapters/vol-{volume_padded}/ch-{chapter_padded}/大纲.md`
4. 与当前冲突最相关的 `memory/future/20-threads.md` 或对应卷卡 / arc 卡

默认不要全量扫项目；只抓这次爆破必须知道的锚点：

- 主角现在最想得到什么
- 谁在压他
- 哪条伏笔最适合爆
- 哪个关系最值得撕裂
- 什么代价一旦发生就无法回头

### 2. 上下文不足时先炸，再说明假设

如果上下文不足，不要先问一串问题。

先基于最近对话和最小锚点给出 10 套方案，再在开头写明你的关键假设。只有在缺少某个核心锚点会导致方案完全失焦时，才允许补问 1 个最关键的问题。

## 爆破方法

### 0. 先生成种子字符串（SSoT）

在构思方案之前，先在心里生成一个复杂随机字符串（至少 30 字符）。用这个字符串的不同片段独立驱动每套方案的核心创作决策，防止多套方案向同一个高概率方向收敛。

具体做法：

1. 生成随机字符串后，按需截取不同片段
2. 每个片段独立驱动对应方案的核心走向选择（从"死亡/失去/诅咒/背叛/身份崩塌/资源清零/关系撕裂/信仰动摇/旧神降临/天象逆转/血祭反噬/梦魇入侵/公开处刑/失控连锁"等类型中选）
3. 任意两套方案的核心类型不得重复；如果抽到已使用的类型，用字符串的下一段重新抽
4. 种子选择过程在 thinking 中完成，不暴露给用户

这一步的目的是确保 10 套方案从根上就走不同的爆破路径，而不是同一个招数换皮。

### 1. 先找最容易炸穿的东西

优先从以下入口下手：

- 主角最在意的人
- 主角刚刚得到的成果
- 读者刚建立的安全感
- 看似稳固的盟约
- 被拖了很久还没回收的伏笔
- 读者默认“暂时不会动”的角色或秩序

### 2. 让代价和收益都变得极端

好的爆破不是单纯更惨，也不是单纯更爽，而是让代价和收益都突然失衡。

可以考虑：

- 用一个死亡换来真相
- 用一次奇迹换来诅咒
- 用提前达成目标换来更大的猎杀
- 用亲密接触、欲望失控或血契绑定，把关系直接推过警戒线
- 用恐怖意象把“危机”升级为“命运被改写”

### 3. 不要停在情绪层，必须改写结构

每套方案都必须真的改写至少一项结构性事实，例如：

- 阵营关系变了
- 主角身份变了
- 目标顺序变了
- 长期敌人提前现身
- 原本要晚些回收的伏笔被强行引爆
- 整卷的终局压力提前压下来

## 输出格式

默认按编号列表输出 10 套方案。每套只需一句话梗概，直接说清楚发生了什么、代价是什么、不可回头点在哪里。

```text
💥 01｜标题——一句话梗概（谁对谁做了什么，导致了什么不可逆的后果）
💥 02｜标题——一句话梗概
...
💥 10｜标题——一句话梗概
```

梗概要求：
- 必须包含具体的角色名和具体的事件，不能只写抽象气氛
- 必须让人一眼看出这套方案跟其他方案的根本区别

## 质量要求

### 必须做到

- 方案之间明显不同
- 至少两套方案必须真正掀桌
- 至少两套方案要让主角付出极大代价
- 至少一套方案可以给出恐怖级红利或奇迹般的助力
- 每套梗概都要具体到能直接继续细化，而不是空喊”加强冲突”

### 明确禁止

- 多数方案都只是”多一个敌人””多一场误会”
- 强行解释为什么最好别这么写
- 自动替作者做道德降温
- 结尾偷塞安全气囊
- 把角色写死以后又立刻反悔
- 用“其实只是幻觉”“其实没出事”“其实大家都活着”把已经炸开的局面抹平

## 落地方式

用户确认其中一条后，再交给 `novel-plan` 落正式大纲。

如果用户还嫌不够狠，就基于已选方案再做第二轮爆破，继续往“更痛、更疯、更不可逆”推，而不是自动收束。
